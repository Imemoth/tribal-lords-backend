import { generateBarbarianVillagesNearby } from '../services/barbarian.service.js';
import jwt from 'jsonwebtoken';
import User from '../models/User.js';
import Village from '../models/Village.js';
import Building from '../models/Building.js';
import { HTTP_STATUS, ERRORS } from '../config/constants.js';

/**
 * AUTH CONTROLLER
 * Regisztráció, bejelentkezés, user info
 */

// ════════════════════════════════════════════════════════════════
// JWT TOKEN GENERÁLÁS (helper függvény)
// ════════════════════════════════════════════════════════════════

const generateToken = (userId) => {
  return jwt.sign(
    { id: userId },                    // Payload: user ID
    process.env.JWT_SECRET,            // Secret kulcs (.env-ből)
    { expiresIn: process.env.JWT_EXPIRES_IN || '7d' }  // Érvényesség
  );
};

// MIT CSINÁL:
// generateToken("user-123")
// → "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6InVzZXItMTIzIn0.xyz..."

// ════════════════════════════════════════════════════════════════
// REGISTER - Új user regisztrációja
// ════════════════════════════════════════════════════════════════

export const register = async (req, res) => {
  try {
    // ──────────────────────────────────────────────────────
    // 1️⃣ ADATOK KINYERÉSE
    // ──────────────────────────────────────────────────────
    
    const { username, email, password } = req.body;
    
    // ──────────────────────────────────────────────────────
    // 2️⃣ VALIDÁCIÓ
    // ──────────────────────────────────────────────────────
    
    // Minden mező kitöltve?
    if (!username || !email || !password) {
      return res.status(HTTP_STATUS.BAD_REQUEST).json({
        success: false,
        error: 'Minden mező kitöltése kötelező'
      });
    }
    
    // Username elég hosszú?
    if (username.length < 3) {
      return res.status(HTTP_STATUS.BAD_REQUEST).json({
        success: false,
        error: 'A felhasználónév legalább 3 karakter kell legyen'
      });
    }
    
    // Jelszó elég hosszú?
    if (password.length < 6) {
      return res.status(HTTP_STATUS.BAD_REQUEST).json({
        success: false,
        error: 'A jelszó legalább 6 karakter kell legyen'
      });
    }
    
    // ──────────────────────────────────────────────────────
    // 3️⃣ USER LÉTREHOZÁSA
    // ──────────────────────────────────────────────────────
    
    const user = await User.create({ username, email, password });
    // User.create():
    // - Ellenőrzi hogy username/email már létezik-e
    // - Jelszót hash-eli (bcrypt)
    // - Elmenti a Map-be
    
    // ──────────────────────────────────────────────────────
    // 4️⃣ KEZDŐ FALU LÉTREHOZÁSA
    // ──────────────────────────────────────────────────────
    
    const village = Village.create({
      userId: user.id,
      name: `${username} falva`,
      x: Math.floor(Math.random() * 100),  // Random koordináta
      y: Math.floor(Math.random() * 100)
    });
    
    // ──────────────────────────────────────────────────────
    // 5️⃣ FALU ALAPÉRTELMEZETT ÉPÜLETEINEK LÉTREHOZÁSA
    // ──────────────────────────────────────────────────────
    
    Building.createDefaultBuildings(village.id);
    // Létrehozza: lumber, clay, iron, headquarters, barracks, wall
    
    // ════════════════════════════════════════════════════════
    // ÚJ: BARBÁR FALVAK GENERÁLÁSA 🏴
    // ════════════════════════════════════════════════════════

    const barbarianCount = 3 + Math.floor(Math.random() * 3); // 3-5 falu
    const barbarianVillages = generateBarbarianVillagesNearby(village, barbarianCount);

    console.log(`[Auth] Created ${barbarianVillages.length} barbarian villages`);

    // ──────────────────────────────────────────────────────
    // 6️⃣ JWT TOKEN GENERÁLÁS
    // ──────────────────────────────────────────────────────
    
    const token = generateToken(user.id);
    // Token-nel tud majd bejelentkezni a frontend
    
    console.log(`[Auth] User registered: ${username} (${user.id})`);
    
    // ──────────────────────────────────────────────────────
    // 7️⃣ VÁLASZ KÜLDÉSE
    // ──────────────────────────────────────────────────────
    
    res.status(HTTP_STATUS.CREATED).json({
      success: true,
      message: 'Sikeres regisztráció',
      token,                    // Frontend elmenti localStorage-ba
      user: user.toJSON(),      // User adatok (jelszó nélkül!)
      village: village.toJSON() // Kezdő falu adatok
    });
    
  } catch (error) {
    console.error('[Auth] Register error:', error);
    
    // User már létezik?
    if (error.message.includes('felhasználónév') || 
        error.message.includes('email')) {
      return res.status(HTTP_STATUS.CONFLICT).json({
        success: false,
        error: error.message
      });
    }
    
    // Egyéb hiba
    res.status(HTTP_STATUS.INTERNAL_ERROR).json({
      success: false,
      error: ERRORS.INTERNAL_ERROR
    });
  }
};

// ════════════════════════════════════════════════════════════════
// LOGIN - Bejelentkezés
// ════════════════════════════════════════════════════════════════

export const login = async (req, res) => {
  try {
    // ──────────────────────────────────────────────────────
    // 1️⃣ ADATOK KINYERÉSE
    // ──────────────────────────────────────────────────────
    
    const { username, password } = req.body;
    
    // ──────────────────────────────────────────────────────
    // 2️⃣ VALIDÁCIÓ
    // ──────────────────────────────────────────────────────
    
    if (!username || !password) {
      return res.status(HTTP_STATUS.BAD_REQUEST).json({
        success: false,
        error: 'Felhasználónév és jelszó megadása kötelező'
      });
    }
    
    // ──────────────────────────────────────────────────────
    // 3️⃣ USER KERESÉSE
    // ──────────────────────────────────────────────────────
    
    const user = User.findByUsername(username);
    
    if (!user) {
      return res.status(HTTP_STATUS.UNAUTHORIZED).json({
        success: false,
        error: ERRORS.INVALID_CREDENTIALS  // "Hibás felhasználónév vagy jelszó"
      });
    }
    
    // ──────────────────────────────────────────────────────
    // 4️⃣ JELSZÓ ELLENŐRZÉSE
    // ──────────────────────────────────────────────────────
    
    const isPasswordValid = await user.comparePassword(password);
    // bcrypt.compare() - összehasonlítja a hash-t
    
    if (!isPasswordValid) {
      return res.status(HTTP_STATUS.UNAUTHORIZED).json({
        success: false,
        error: ERRORS.INVALID_CREDENTIALS
      });
    }
    
    // MIÉRT UGYANAZ A HIBAÜZENET?
    // Biztonsági okokból! Ne áruljuk el, hogy a username létezik-e.
    
    // ──────────────────────────────────────────────────────
    // 5️⃣ LAST LOGIN FRISSÍTÉSE
    // ──────────────────────────────────────────────────────
    
    User.updateLastLogin(user.id);
    
    // ──────────────────────────────────────────────────────
    // 6️⃣ JWT TOKEN GENERÁLÁS
    // ──────────────────────────────────────────────────────
    
    const token = generateToken(user.id);
    
    // ──────────────────────────────────────────────────────
    // 7️⃣ USER FALVAINAK LEKÉRÉSE
    // ──────────────────────────────────────────────────────
    
    const villages = Village.findByUserId(user.id);
    
    console.log(`[Auth] User logged in: ${username} (${user.id})`);
    
    // ──────────────────────────────────────────────────────
    // 8️⃣ VÁLASZ KÜLDÉSE
    // ──────────────────────────────────────────────────────
    
    res.json({
      success: true,
      message: 'Sikeres bejelentkezés',
      token,
      user: user.toJSON(),
      villages: villages.map(v => v.toJSON())
    });
    
  } catch (error) {
    console.error('[Auth] Login error:', error);
    res.status(HTTP_STATUS.INTERNAL_ERROR).json({
      success: false,
      error: ERRORS.INTERNAL_ERROR
    });
  }
};

// ════════════════════════════════════════════════════════════════
// GET ME - Aktuális user adatai
// ════════════════════════════════════════════════════════════════

export const getMe = async (req, res) => {
  try {
    // ──────────────────────────────────────────────────────
    // 1️⃣ USER ID LEKÉRÉSE
    // ──────────────────────────────────────────────────────
    
    // req.userId → authMiddleware-ből jön!
    // A middleware dekódolta a JWT tokent és hozzáadta a request-hez
    
    const user = User.findById(req.userId);
    
    if (!user) {
      return res.status(HTTP_STATUS.NOT_FOUND).json({
        success: false,
        error: 'User nem található'
      });
    }
    
    // ──────────────────────────────────────────────────────
    // 2️⃣ USER FALVAINAK LEKÉRÉSE
    // ──────────────────────────────────────────────────────
    
    const villages = Village.findByUserId(user.id);
    
    // ──────────────────────────────────────────────────────
    // 3️⃣ VÁLASZ KÜLDÉSE
    // ──────────────────────────────────────────────────────
    
    res.json({
      success: true,
      user: user.toJSON(),
      villages: villages.map(v => v.toJSON())
    });
    
  } catch (error) {
    console.error('[Auth] GetMe error:', error);
    res.status(HTTP_STATUS.INTERNAL_ERROR).json({
      success: false,
      error: ERRORS.INTERNAL_ERROR
    });
  }
};

// ════════════════════════════════════════════════════════════════
// EXPORT
// ════════════════════════════════════════════════════════════════

export default {
  register,
  login,
  getMe
};
